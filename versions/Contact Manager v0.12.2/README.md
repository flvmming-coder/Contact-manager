# Contact Manager v0.12.2 (TEST)

UI update for testing:
- FAB button now uses amber gradient (`#f59e0b -> #fbbf24`) and stays in bottom-right corner.
- Header icon to the left of `Contact Manager` uses the same amber gradient.
- Contacts/imported stats color changed to `#fbbf24`.
- Contact cards now have rounded corners and thin border `#64748b`.
- Group filters are shown as a horizontal scroll row under search.
- `All` is selected by default on the left.
- Selected group is highlighted with amber gradient (`#f59e0b -> #fbbf24`).

APK:
- `ContactManager-v0.12.2-debug.apk`

Status:
- test build for visual and UX validation.
